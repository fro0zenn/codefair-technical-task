// src/theme.ts

import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    // Customize your theme here (e.g., typography, colors, spacing, etc.)
});

export default theme;
